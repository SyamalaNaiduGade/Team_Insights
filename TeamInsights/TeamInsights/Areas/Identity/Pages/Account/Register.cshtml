@page
@model RegisterModel

@{
    ViewData["Title"] = "Create an Account";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Team Insights</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/TeamInsights.styles.css" asp-append-version="true" />
</head>
<body class="dark-theme">

    <div class="container d-flex justify-content-center align-items-center vh-100">
        <div class="card shadow-lg p-4 login-card">
            <h2 class="text-center mb-3 text-dark">Register</h2>

            <form id="registerForm" method="post" asp-route-returnUrl="@Model.ReturnUrl">
                <div asp-validation-summary="ModelOnly" class="text-danger text-center mb-3" role="alert"></div>

                <div class="mb-3">
                    <label asp-for="Input.Email" class="form-label text-dark">Email</label>
                    <input asp-for="Input.Email" class="form-control custom-input" autocomplete="username" placeholder="Enter your email address" required />
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Input.Password" class="form-label text-dark">Password</label>
                    <input asp-for="Input.Password" type="password" class="form-control custom-input" autocomplete="new-password" placeholder="Enter your password" required />
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Input.ConfirmPassword" class="form-label text-dark">Confirm Password</label>
                    <input asp-for="Input.ConfirmPassword" type="password" class="form-control custom-input" autocomplete="new-password" placeholder="Re-enter your password" required />
                    <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
                </div>

                <button type="submit" class="btn btn-primary w-100 py-2 custom-button">
                    Register
                </button>

                <div class="text-center mt-3">
                    <p>
                        <a asp-page="./Login" asp-route-returnUrl="@Model.ReturnUrl" class="text-decoration-none text-primary link-hover">Already have an account? Login</a>
                    </p>
                </div>
            </form>
        </div>
    </div>

    <footer class="text-muted text-center mt-4 text-light">
        &copy; 2025 - Team Insights - <a asp-area="" asp-controller="Home" asp-action="Privacy" class="text-light">Privacy</a>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <partial name="_ValidationScriptsPartial" />

    <style>
        /* Dark Theme Background */
        .dark-theme {
            background-color: #000;
            color: #fff;
        }

        /* White Form Card */
        .login-card {
            background: #fff;
            border-radius: 10px;
            width: 100%;
            max-width: 400px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
        }

        /* Input Fields */
        .custom-input {
            background: #fff;
            border: 2px solid #ddd;
            color: #333;
            border-radius: 8px;
            transition: 0.3s;
            padding: 10px;
        }

            .custom-input:focus {
                border-color: #007BFF;
                box-shadow: 0 4px 8px rgba(0, 123, 255, 0.3);
            }

        /* Button */
        .custom-button {
            background: linear-gradient(to right, #007BFF, #0056B3);
            border: none;
            color: white;
            font-weight: bold;
            transition: 0.3s;
        }

            .custom-button:hover {
                background: linear-gradient(to right, #0056B3, #003F7F);
            }

        /* Links */
        .link-hover:hover {
            text-decoration: underline;
            color: #007BFF;
        }
    </style>

</body>
</html>
